// See https://aka.ms/new-console-template for more information
using BookStore;
using BookStore.Auth;
using BookStore.AuthorizationAndRegistration;
using BookStore.Serialize;
using BookStore.Services;
using System.Net;

//FileStorage fileStorage = new FileStorage();
//Store store = new Store(fileStorage);
//Validator validator = new Validator(store);
//Registration registration = new Registration(store, new BookStore.AgePolicy.UserAge(),validator);
//BookStore.AuthorizationAndRegistration.Authorization authorizationUser = new BookStore.AuthorizationAndRegistration.Authorization(store, validator);
//await store.Initialize();
//UI uI = new UI(registration,authorizationUser);
//uI.StartWindow();




//foreach (var item in store.Users)
//{
//    Console.WriteLine($"ID: {item.ID}, Name: {item.FirstName}");
//}



